


export default function Type() {

    /* 
    
    fetch fra API --> types
    I return --> mappe ut API-fetch noe ale dette {pokemon?.map((card, index) => {return <Card  key={index} card={card}/>;})}
    */

    return (
        <>
        <h1>Pokemon types map...</h1>
        </>
    )
}